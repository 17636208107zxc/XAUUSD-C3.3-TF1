# 代码与发布审计记录

## 已解决 1：C3.3-TF1 头文件定位与编译

文件：`src/XAUUSD_M15_M5_V3_10_9_C33_TF1_TREND_FREQUENCY.mq5` 第 15–34 行。
位置：`Include/XAUAI/V3109C33TF1/`。
现象：首次常规搜索跳过了隐藏 `.worktrees` 目录，误判为缺失；完整磁盘搜索在 `v3109-c32-multi-entry` 工作树中找到 23 个同版头文件。
当前是否实际执行：主 EA 直接引用其中 20 个；其余包含传递依赖与同版辅助模块。
潜在影响：误用旧版同名文件会造成编译或策略不一致。
本次处理：仅复制同版 `.mqh` 到独立发布目录，23 个哈希逐一一致；保留当前终端主 EA 不变。MetaEditor 对发布目录编译结果为 **0 errors、0 warnings**。

## 问题 2：主文件顶部部署注释与当前 Include 清单不一致

文件：同一主 EA 第 9–10 行及第 15–34 行。
现象：顶部历史注释写“仅依赖 MT5 自带标准库”，但实际声明了 20 个项目头文件。
当前是否实际执行：注释不执行，Include 声明影响编译。
潜在影响：协作者可能误以为单文件可编译。
本次处理：仅记录，未修改源码；README 以实际 `#include` 为准。

## 问题 3：运行配置含真实 Webhook 和账户标识

文件：本机辅助服务 `config/review_config_v3109.json` 与 `config/review_config.json`，以及 MT5 Common/Files 运行目录。
现象：检测到真实飞书机器人 Webhook；运行配置另含预期 MT5 登录标识及本机路径。
当前是否实际执行：取决于本机辅助服务的运行配置。
潜在影响：直接复制运行目录到 GitHub 会泄漏通知入口及账户隐私。
本次处理：原文件未改、未删除、未复制到发布区；发布区只提供无凭证模板。

## 问题 4：历史参数与函数不等于当前规则

文件：主 EA 中 `InpMaxSignalBarUSD`、`InpMaxSLATR`、旧 Signal Engine、Countertrend 相关代码。
现象：参数和旧函数仍在长文件中；C33 主链路又明确关闭普通逆势战术许可。
当前是否实际执行：各参数是否作用于 C33 主入口需逐级核对，不能仅凭出现次数判断。
潜在影响：AI 或人工可能把历史代码误写成当前策略。
本次处理：仅记录，未修改。

## 问题 5：当前专属 `.set` 参数文件未找到

文件：当前 MT5 终端 `MQL5/Presets/`。
现象：找到 C32B `.set`，未找到名称对应 C33-TF1 的 `.set`。图表实际挂载输入值可能与源码默认值不同。
当前是否实际执行：无法仅凭源码判断挂载时是否覆盖。
潜在影响：协作者无法用仓库材料精确复现实盘设置。
本次处理：仅记录；不以 C32B 参数文件冒充 C33。

## 问题 6：本机路径脚本与旧版发布测试不适合直接入库

文件：原辅助项目中的 `self_check_remediation.py`、`verify_v3109_m15.py`、`verify_v3109_m5.py` 及相关测试；另有部分测试依赖旧 V3.8 EA 和旧 Include 目录。
现象：前三个脚本含硬编码本机账户路径；旧版发布测试在仅含 C33 主 EA 的项目中会因缺少旧文件而失败，与 C33 功能无关。
当前是否实际执行：本机辅助服务的自检任务可能调用本地维护脚本；GitHub 发布区不提供它们。
潜在影响：直接复制会泄露账户路径并让协作者误把旧版测试当成 C33 验证。
本次处理：只从独立发布准备区排除这些副本及其对应测试，原工作项目未删除或修改。发布区保留的 Python 测试为 379 passed；它与 EA 编译属于两项不同验证。

## 问题 7：两份 C33 主 EA 的 AI 默认值不同

文件：当前 MT5 终端主 EA 与隐藏开发工作树中的同名主 EA。
现象：终端版默认 `InpAIMode=AI_OFF`、模型 `deepseek-v4-flash`；开发工作树版默认 `InpAIMode=AI_LIVE`、模型 `deepseek-flash`。其余文本差异检查未见交易策略逻辑变动。
当前是否实际执行：实际图表输入值可能覆盖源码默认值；不能仅凭默认值判断当前账户是否调用 AI。
潜在影响：若把开发工作树主文件覆盖终端版，会改变 AI 默认行为。
本次处理：发布目录坚持使用终端版主 EA，仅从开发工作树复制同版 Include；两份主文件均未修改。
